#include <stdio.h>
/**
 * main - Entry of main function
 *
 * Return: 0 (Success)
 */
int main(void)
{
	char q;
	char w;

	q = 'a';
	w = 'A';
	while
		(q <= 'z') {
			putchar(q);
			q++;
		}
	while
		(w <= 'Z') {
			putchar(w);
			w++;
		}
	putchar('\n');
	return (0);
}
