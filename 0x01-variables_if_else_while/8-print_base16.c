#include <stdio.h>
/**
 * main - program enters and begins here
 *
 * Return: 0 (Success)
 */
int main(void)
{
	char z;

	char e;

	z = 0;
	e = 'a';
	while
		(z <= 10) {
			putchar(z + '0');
			z++;
		}
	while
		(e <= 'f') {
			putchar(e);
			e++;
		}
	putchar('\n');
	return (0);
}
