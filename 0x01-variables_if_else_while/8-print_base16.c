#include <stdio.h>
/**
 * main - program enters and begins here
 *
 * Return: 0 (Success)
 */
int main(void)
{
	char e;

	char z;

	e = 'a';
	z = 0;
	while
		(z <= 10) {
			putchar(z + '0');
			z++;
		}
	while
		(e <= 'f') {
			putchar(e);
			e++;
		}
	putchar('\n');
	return (0);
}
