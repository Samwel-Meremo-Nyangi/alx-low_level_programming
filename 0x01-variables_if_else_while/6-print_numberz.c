#include <stdio.h>
/**
 * main - entry of new program
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int m;

	m = 0;
	while
		(m < 10) {
			putchar(m + '0');
			m++;
		}
	putchar('\n');
	return (0);
}
