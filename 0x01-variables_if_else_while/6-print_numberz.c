#include <stdio.h>
/**
 * main - program starts here at entry point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	char x;

	x = 'z';
	while
		(x >= 'a') {
			putchar(x);
			x--;
		}
	putchar('\n');
	return (0);
}
