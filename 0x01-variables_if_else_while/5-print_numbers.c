#include <stdio.h>
/**
 * main - Start of program where code starts to execute
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int v;

	for (v = 0; v < 10; v++)
		printf("%d", v);
	putchar('\n');
	return (0);
}
