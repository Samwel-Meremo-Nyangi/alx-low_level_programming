#include <stdio.h>
/**
 * main - This is the entry to a program and its function
 * puts - prints the set of characters
 * return 0 - (Success)
 */
int main(void)
{
puts("\"Programming is building a multilingual puzzle");
return (0);
}
