#include "main.h"
/**
 * _abs - calcu;ates integer modularity and absolute value
 * @n: checked
 * Return: absolute value of n
 */
int _abs(int n)
{
        if (n >= 0)
        {
                return (n);
        }
        return (-n);
}
